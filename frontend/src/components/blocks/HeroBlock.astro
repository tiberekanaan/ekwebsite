---
import { Image } from 'astro:assets';
// Real photo, pre-cropped 4:5 and graded to the ek palette — regenerate via
// sharp (1200x1500, teal soft-light overlay) if the source photo changes.
import heroPhoto from '@/assets/images/hero-photo.jpg';

export interface HeroBlockData {
  title?: string | null;
  description?: string | null;
  primaryButtonText?: string | null;
  primaryButtonLink?: string | null;
  secondaryButtonText?: string | null;
  secondaryButtonLink?: string | null;
}

interface Props {
  block: HeroBlockData;
}

const { block } = Astro.props;

const primaryText = block.primaryButtonText ?? 'Support our work';
const primaryLink = block.primaryButtonLink ?? '/contact';
const secondaryText = block.secondaryButtonText ?? 'See our 2026–2030 vision';
---

<section
  id="top"
  class="relative flex min-h-svh flex-col overflow-hidden bg-ek-900 bg-[radial-gradient(1200px_600px_at_75%_-10%,rgba(42,128,137,0.55),transparent_60%),linear-gradient(160deg,var(--color-ek-800)_0%,var(--color-ek-900)_70%)] text-white"
>
  <svg
    class="pointer-events-none absolute -top-[6%] right-[-8%] z-0 w-[600px] opacity-[0.06]"
    viewBox="0 0 200 200"
    aria-hidden="true"
  >
    <path d="M100 20c-25 0-45 20-45 50s20 60 45 60 45-30 45-60-20-50-45-50z" fill="#aecb3a" />
  </svg>

  <div class="relative z-[2] mx-auto grid w-full max-w-6xl flex-1 items-center gap-10 px-7 pt-28 pb-8 lg:grid-cols-[1.15fr_0.85fr] lg:pt-32">
    <div>
      <p
        data-reveal
        class="mb-5 font-condensed text-[0.78rem] font-semibold uppercase tracking-[0.22em] text-ek-lime-bright"
      >
        A locally-led NGO · Kiribati
      </p>

      <h1
        data-reveal
        data-reveal-delay="80"
        class="mb-6 bg-gradient-to-br from-white via-white to-ek-lime-bright bg-clip-text font-serif text-5xl font-bold leading-[1.06] text-transparent sm:text-6xl lg:text-7xl"
      >
        {
          block.title ? (
            block.title
          ) : (
            <Fragment>
              Cultivating a <em class="not-italic text-ek-lime-bright">Resilient</em> Kiribati
            </Fragment>
          )
        }
      </h1>

      <p data-reveal data-reveal-delay="160" class="mb-[18px] max-w-[30em] text-lg text-[#cfe0db]">
        {
          block.description ??
            'We help our people gain the leadership, enterprise, and digital skills they need to thrive in a rapidly changing world — and to lead that change themselves.'
        }
      </p>

      <p
        data-reveal
        data-reveal-delay="220"
        class="mb-9 max-w-[34em] border-l-2 border-ek-olive pl-[18px] text-base text-[#9fb9b4]"
      >
        For generations, our people have grown the bwabwai in harsh atoll soils — work that has
        always demanded knowledge, planning, and constant adaptation. Today our communities face the
        same test, and need new tools to meet it.
      </p>

      <div data-reveal data-reveal-delay="300" class="flex flex-wrap items-center gap-4">
        <a
          href={primaryLink}
          class="inline-flex items-center gap-2 rounded-full bg-ek-lime px-[22px] py-3 font-condensed text-[0.92rem] font-semibold uppercase tracking-[0.06em] text-ek-900 transition-all duration-200 hover:-translate-y-0.5 hover:bg-ek-lime-bright hover:shadow-[0_8px_22px_rgba(174,203,58,0.3)]"
        >
          {primaryText}
        </a>
        <a
          aria-disabled="true"
          class="pointer-events-none inline-flex cursor-not-allowed items-center gap-2 rounded-full border-[1.5px] border-white/35 px-[22px] py-3 font-condensed text-[0.92rem] font-semibold uppercase tracking-[0.06em] text-white opacity-50"
        >
          {secondaryText}
        </a>
      </div>
    </div>

    <div class="relative hidden justify-center pb-4 lg:flex" data-reveal data-reveal-delay="200">
      <div class="relative aspect-[4/5] w-[min(100%,400px)] overflow-hidden rounded-2xl shadow-[0_24px_60px_rgba(0,0,0,0.4)] ring-1 ring-white/15">
        <Image
          src={heroPhoto}
          alt="Children laughing together in a game under the roof of a maneaba in Kiribati"
          widths={[400, 800]}
          sizes="(min-width: 1024px) 400px, 100vw"
          class="absolute inset-0 h-full w-full object-cover"
          loading="eager"
        />
      </div>
    </div>
  </div>

</section>
